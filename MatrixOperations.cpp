#include "MatrixOperations.h"
#include "Matrix.h"

MatrixOperations::Add(Matrix A, Matrix B)
{
  //
}

MatrixOperations::Multiply(Matrix A, Matrix B)
{
  //
}

MatrixOperations::Invert(Matrix A)
{
  //
}

MatrixOperations::Print(Matrix A)
{
  int rows = A.get_rows();
  int columns = A.get_columns();

  //Just print it with a nested loop?
  /*
  for(int i=0; i<rows; i++)
  {
    for(int j=0; j<columns; j++)
    {

    }
  }
  */

}
